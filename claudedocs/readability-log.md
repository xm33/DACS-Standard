# DACS Readability / Clarity Log

Running list of wording/structure issues found while reading the spec, to review and fix in one pass at the end. **No normative meaning changes** — these are *how it reads*, not *what it says*.

Two categories:
- **PL — plain-language**: reader-facing prose (Primer, module Abstracts/Motivation/Rationale). Fix = simpler words.
- **SC — structural-clarity**: dense *normative* text where precision is load-bearing. Fix = better structure (define-once + bullets), wording stays exact.

Status: `open` → `fixed`.

**DACS-1 (Identify): pass complete 2026-06-06** — first 7 items (read-through finds) fixed, THEN a comprehensive whole-module sweep added #8–#11 and caught a self-inflicted duplication. 27/27 rule IDs + 103/103 MUSTs preserved throughout, 5 validators + 35 tests green.

**Comprehensive sweep (2026-06-06):** assessed all 14 prose paragraphs ≥110 words. Fixed #8–#11 below. **Corrected a duplication SC#7 introduced** — my CF-4 table came with a "**The rule.**" sentence that duplicated the pre-existing formal **Rule CF-4** (§6.3.4 L422); removed it, folded the MUST/MUST-NOT into the table lead-in. The other ~8 dense paragraphs are single-topic/coherent or already-structured (session-nonce binding, selective-disclosure scope note, SIWD-preferred, sr1-root, revocation, §6.6 security threats, the write-input-mapping (a)(b)(c) block, the formal Rule CF-4 with its worked example, the CF-2 (a)(b)(c) bullet) — left as-is (not the fused-rule+enumeration anti-pattern).

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 1 | DACS-1 §6.1 (abstract, "identity claim reference scheme" bullet) | PL | "a typed reference to an external identifier … optionally paired with a DACS-2 verification method" — jargon-dense for an abstract | "A way to **name an identity that already exists somewhere else** (a domain, DID, company LEI, Google account, signing key), written as `type:value` (e.g. `lei:5493…`). Each name can optionally carry **proof it was checked** against that source." | fixed |
| 2 | DACS-1 §6.1 (abstract, listing bullet) | PL | "The listing is the canonical contract for any transaction against the seller." — "canonical contract" unexplained | "The listing is the seller's **signed, pinned statement of terms** — the single source of truth every deal with that seller is checked against." | fixed |
| 3 | DACS-1 §6.3.1 (CF-2/CF-3, "A ClaimReference has a canonical byte form — what is embedded …") | SC | "what is embedded" / "what is compared" reads like a question mid-sentence | "a *canonical byte form* — **the bytes embedded** whenever the reference appears in a hashed or signed document … — and a *canonical identity* — **the value compared** for matching, reputation keying, and the §7.3.2 replay defence." | fixed |
| 4 | DACS-1 §6.3.2 (PresentationSignature signed payload paragraph) | SC | One wall of text mixing one idea + four byte-exact rules; very hard to parse | Define `signed_bytes` once up front, then one bullet per kind (per-claim / session-key / sr1-root / siwd). Keep every byte-exact rule verbatim. Draft in chat 2026-06-05. | fixed |
| 5 | DACS-1 §6.3.2 (4-tier ranking, inline in the presentedBy sentence) + §6.3.2.1 (3-tier collapse) + §6.3.1 (per-scheme hints) | SC | Claim tiers have **no single citable definition** — the 4-level ranking is buried mid-sentence in §6.3.2, the 3-level IT collapse is restated in §6.3.2.1, scheme hints are scattered in §6.3.1. Reader must reconcile across 3 places, and the 4-tier vs 3-tier views can drift (§6.3.2.1 manually asserts "this is the collapse of §6.3.2"). | Extract a **define-once "Claim tiers" table** (4 levels → schemes → 3-level IT collapse) that §6.3.2 and §6.3.2.1 both *reference* instead of restating. Table drafted in chat 2026-06-05: `1 Authority-issued (lei/finra-crd/sam-uei/fedramp/cmmc/naics)→institutional · 2 DID/ERC-8004+proof→verified · 3 Platform→verified · 4 Plain key→self-declared`. | fixed |

| 6 | DACS-1 §6.3.2 ("For a BundleClaim with verifiedBy present: …" + the freshness-window paragraph) | SC | One dense paragraph fuses *two* things: the 5-step verification checklist **and** the freshness-window formula (with clamping + fail-closed). The inline `min(…)` and the "ignore later wrapper timestamps" clamp are very hard to parse in prose. | Split into (1) a numbered **verification steps** list (fetch → hash-check → parse+recipe → identifier-match → decision==pass → else unverified), and (2) a **Freshness window** sub-block: issuance = `verifiedAt`; `expiry = min(expiresAt ?? ∞, validUntil ?? verifiedAt+defaultMaxAgeSec×1000)`; presenter timestamps clamp (narrow-only, never widen); `defaultMaxAgeSec` from the result's own `recipeVersion` (not latest); fail-closed when undeterminable. Keep the formula verbatim. | fixed |

| 7 | DACS-1 §6.3.4 (CF-4 logical-address encoding paragraph) | SC | One dense paragraph carries: what CF-4 is/isn't + the full list of 7 address kinds it applies to + which segment is variable in each + the why. The per-address variable-vs-fixed detail is impossible to scan in prose. | Lead with the one rule (encode internal `:` of variable segments → `%3A`; leave structural delimiters), then a **table** (address → variable segment(s) → fixed segments), then the one-line why (undecidable boundaries → divergent native addresses). Note CF-4 governs the *name's* parseability, not the native-address formula. Keep all address patterns verbatim. Table drafted in chat 2026-06-06. | fixed |

| 8 | DACS-1 §6.3.2 (presentedBy selection paragraph, 206w) | SC | Fuses the selection rule + reader-acceptance + the big "Reputation MUST NOT be keyed against an unverified presentedBy" anti-laundering rule in one 206-word block | Split: selection-rule paragraph + a separate **Verified-presentedBy for reputation** rule. | fixed |
| 9 | DACS-1 §6.3.4 (listing validation order) | SC | "validate in the following order, halting on first failure: (1)…(9)" — an ordered sequence flattened into prose | Numbered list (1–9), each step verbatim. | fixed |
| 10 | DACS-1 §6.3.2 (canonical serialisation paragraph) | SC | General canonical-form/hash rule fused with the SIWD-specific verifier re-derivation MUST | Split: general rule + a separate **SIWD bundle-binding check** block. | fixed |
| 11 | DACS-1 §6.3.2 (BP-1..BP-4 / BR-1..BR-5 conformance) | SC | Inline `(BP-1) … (BP-4)` and `(BR-1) … (BR-5)` lists in running prose | Bullet lists, one rule per bullet, verbatim. | fixed |

## Post-pass review (fresh-eyes, 2026-06-06)
A read-only critical review of the edited DACS-1 (independent reviewer) found **two contradictions the readability pass introduced/hardened** — both fixed:
- **C1 (critical).** The new **Claim tiers** table carried an `identityTier` column mapping `key` → `self-declared`, but the §6.3.2.1 derivation (step 2) maps a *verified* `key:` → `verified`. They are **different axes** (scheme-strength for `presentedBy` vs verification-status for `identityTier`) that SC#5 wrongly merged (a latent pre-restructure tension the table hardened into an authoritative contradiction). Fix: dropped the `identityTier` column from the tiers table; reworded the governance line + the §6.3.2.1 sentence to state the two axes distinctly (verified `key:` → `verified`).
- **C2 (minor).** Rule CF-4 prose listed `listingId` as MUST-percent-encode, but the new CF-4 table (and worked example) treat it as fixed. `listingId` is URL-safe ASCII (§6.3.4) so carries no reserved delimiters — fixed the prose to match the table.

Everything else the restructure touched verified behavior-preserving (signed-bytes bullets, BP/BR, validation list, verify+freshness, CF-4 table all sound; tables well-formed; no dropped MUSTs). Lesson: **a readability table that adds a *derived* column can encode a contradiction the source prose only had latently — always cross-check new tables against the surviving derivation rules.**

## Post-pass review — round 2 (independent, cold, 2026-06-06)
A second fresh-eyes reviewer (no knowledge of C1/C2) read DACS-1 cold. **Clean on criticals** — explicitly confirmed the C1/C2 fixes hold (verified-`key:` → `verified` consistent in all 3 places; CF-4 `listingId` consistent across prose/table/example; no false "cannot disagree" claim; no dropped MUSTs; rule families single-defined; tables well-formed). Two **minor** items fixed:
- §6.4 rationale listed **"OFAC"** among shipped DACS-1 schemes — but OFAC is a DACS-2 sanctions *check*, not an identity scheme (**pre-existing error from the monolith**, count 1 on main). Corrected to the real registry (LEI, FINRA-CRD, SAM-UEI, FedRAMP, NAICS, CMMC).
- `BundleClaim.expiresAt` schema comment "when verifiedBy becomes stale" overstated the field (it only *narrows* the authority window) — aligned to §6.3.2.
- (no-op) two `signed_bytes :=` definitions share the name but are disambiguated by their domain prefixes — cosmetic, left.

**Convergence:** round 2 found no new criticals → DACS-1 treated as clean.

## DACS-2 (Vet) — pass complete 2026-06-07

Read-through finds #12–#21 fixed, THEN a systematic ≥110-word sweep added #22–#26 (the read-through missed five more instances of the **same** inline-rule-list / semicolon-procedure patterns — RA, RAV, WN, §7.5.2, §7.6 — exactly the DACS-1 lesson). 45/45 rule IDs + 97/97 MUSTs (26 MUST NOT) preserved throughout; 5 validators + 35 tests green. Two cold fresh-eyes reviews ran (see below) — no introduced defects.

**#14 was rescoped during application (important catch).** The log proposed hoisting the session-nonce binding into a blanket common-contract rule (e.g. CM-6) applying to *every* method. On re-reading, only **VC (§7.3.2) and oauth (§7.3.6)** carry it — and they *should*, because the other six methods (proxy/evm-rpc/tlsnotary/zktls/domain-tls/self-signed) have no replayable counterparty-supplied attestation. A blanket rule would have been a **normative expansion**, not behavior-preserving, and would have changed the rule-ID set. Instead: VC is the canonical statement (new *Session-nonce binding* sub-block), and oauth **references** it rather than restating the §6.3.2 disjunction. No new rule ID; the dedup *reduced* duplication (the disjunction phrase now appears once).

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 12 | DACS-2 §7.3.1 (method common contract, CM-1..CM-5) | SC | Inline `(CM-1) … (CM-5)` list in one running sentence — same hard-to-scan pattern as DACS-1 BP/BR | Bullet list, one CM rule per bullet; keep the CM-2 address `dacs2:{jobId}:{scheme}:{identifier}:v{recipeVersion}` + CF-4 note verbatim. | fixed |
| 13 | DACS-2 §7.3.2 (verifiable-credential `Procedure`, 278w) | SC | One semicolon-chained sentence fusing: the verification step sequence + the holder-binding security rule (incl. the §6.3.2 nonce disjunction + its rationale) + the 4-way pass/fail/error/indeterminate outcome mapping + trust-model/substrate footer | Numbered **verification steps** list; pull holder-binding into its own labelled sub-block (the MUST + nonce disjunction + the replay rationale); render the outcome mapping as a small pass/fail/error/indeterminate list. Keep all MUSTs verbatim. **NB: all 8 method subsections §7.3.2–7.3.9 are "Procedure." prose of this shape — assess each in the DACS-2 sweep.** | fixed |

| 14 | DACS-2 §7.3.2–7.3.9 (every method `Procedure` restates the session-nonce binding) | SC (define-once) | The session-nonce anti-replay binding (the §6.3.2 disjunction: SIWD Nonce else `bundleToVet.sessionNonce`, reject on mismatch) is **repeated in each method procedure** — VC calls it "holder-binding" (§7.3.2), oauth-attested restates it (§7.3.6 "consistent with §7.3.2"), etc. "Describe it N times" at the method level. | **Define once** in the common contract — add a CM rule (e.g. CM-6 "every method MUST bind its attestation to the session nonce per §6.3.2") — and have each method `Procedure` reference it + state only its method-specific caveat (e.g. oauth's "if the service can't carry a nonce, document residual replay risk"). Bigger dedup than bulletizing each procedure (which is #13). | fixed |

| 15 | DACS-2 §7.3.x (PSP-2 "Decision mapping") | SC | A 2-axis decision table (parse-result × predicate-match, with the positive/negative-match polarity inversion via `negativeMatch: true`) **flattened into prose**, plus the `indeterminateOn`-runs-first ordering rule and the per-format predicate-kind list, all in one block. The polarity inversion (match=pass for `lei` vs match=fail for `ofac-clear`) is the load-bearing concept and is buried mid-sentence. | Lead with the polarity concept (positive vs negative match, `negativeMatch:true` flips it). Render the mapping as a **table** (rows: parses+match / parses+no-match / doesn't-parse / authority-inconclusive; columns: positive scheme, negative scheme → pass/fail/error/indeterminate). State the `indeterminateOn`-before-match ordering as a one-line rule above it. Keep predicate-kind list (jsonPath/selector/xPath/matcher) as a small bullet. Values verbatim. | fixed |
| 16 | DACS-2 §7.3.x (PSP-5 "Negative-match completeness floor") | SC | One dense block fusing the rule (MUST confirm completeness before a negative `pass`) + the three accepted completeness signals + the MUST-NOT/→indeterminate + the worked rationale (truncated SDN clears a sanctioned party) + the scope line. | Rule-first sentence ("for a negative-match scheme deciding by *absence* in a downloaded list, MUST prove the download complete before `pass`"); bullet the three completeness signals (declared count / end-of-list sentinel / Content-Length==bytes); separate **Why** callout (can't prove a negative over a partial list; truncation would clear a sanctioned entity); one-line scope (`negativeMatch:true` + "identifier not found in downloaded list"). Verbatim. | fixed |

| 17 | DACS-2 §7.4.x (Progressive anchoring phases PA-1/2/3 + GOV-2/GOV-3) | SC | One block fuses: 3 anchoring phases + the append-only re-anchoring invariant + the pin-time validation rule (pinned recipeVersion validates against the phase/key in force *at pin time*) + GOV-2/GOV-3. Also `anchoring: "in-code"` is **used in the deprecation sentence before PA-1 defines it** — caused real reader confusion ("what are in-code?"). | Define the three `anchoring` values **up front in a small table** (in-code / single-signer / multisig → what it means → trust level → which is current v0.1), before the deprecation sentence uses the value. Then PA-1/2/3 as the phase rules, the **append-only + pin-time invariant** as its own callout (immutable old versions, re-anchor only as new versions, pinned result validates against pin-time phase/key per §7.12/§12.4), GOV-2/GOV-3 as two bullets noting GOV-3 is satisfied by `governance.anchoring` (§7.4.1). Verbatim values. | fixed |

| 18 | DACS-2 §7.6.1 (retry policy VP-R1..VP-R4) | SC | A small **decision matrix** (outcome × retryClass → retry?) written as four prose rules; the unifying principle ("only retry your-side transient failures; an authority's substantive answer is final") is implicit. | Lead with the principle, then a **table**: rows error+transient / error+permanent / indeterminate / fail / pass → retry? + budget. Keep VP-R2 (new attestation) and VP-R4's `retryOnIndeterminate` escape-hatch (pending→conclusive) as riders. Values verbatim (default 3, exp backoff; retryOnIndeterminate default false). | fixed |
| 19 | DACS-2 §7.6.1 (reuse/caching VP-C1..VP-C3) | SC | VP-C1 fuses the reuse-window formula + the "validUntil wins, defaultMaxAgeSec is fallback (from the pinned recipeVersion)" gloss + the §6.3.2 consistency cross-reference + the no-analogue-clamp parenthetical, all in one sentence. | Lead with the formula `now ≤ validUntil ?? (verifiedAt + defaultMaxAgeSec×1000)` as a display block + one-line gloss (validUntil wins; fallback from *that* recipeVersion). Move the §6.3.2-agreement + clamp-has-no-analogue into a **consistency note** callout. Keep VP-C2 (record reference) + VP-C3 (listing `ClaimRequirement.maxAge` can tighten, never bypass) as two bullets. Verbatim. | fixed |

| 20 | DACS-2 §7.7.1 (composite-record anchoring/signature) | SC | One sentence carries three independent MUSTs (anchor via SR-2 / record anchor-ref in DACS-5 session record / sign by verifier over §B.7 payload) **plus** the CF-4 encoding requirement for `{evaluatedParty}` nested in a parenthetical inside the address clause + an inline "verifier (= party running Vet)" gloss. | Split into three short rules: (1) **Anchor** — address `dacs2:composite:{jobId}:{evaluatedParty}` as a display block, with the CF-4 note beneath (`{evaluatedParty}` is a ClaimReference/CF-4 variable segment → percent-encode before assembly, §6.3.4); "or substrate equivalent" kept. (2) **Record** anchor reference in the DACS-5 session record. (3) **Sign** — verifier (party running Vet), §B.7 domain-separated payload. Verbatim. NB: same `dacs2:`+CF-4-variable-segment shape as CM-2 (#14/§10 CF-4-to-Core theme). | fixed |

| 21 | DACS-2 §7.8.2 (orchestrator vet-phase contract VPC-1..VPC-4) | SC | Inline `(VPC-1)…(VPC-4)` list; **VPC-2's cross-check** (buyer-bundle vs listing-side-reqs-on-buyers / seller-bundle vs buyer-side-reqs-on-sellers) is hard to follow; **VPC-4** fuses a 3-row fault-attribution mapping + a big EXCEPT clause (unparseable counterparty presentation → counterparty) + the "fault-attribution-only" note + the "why no transient class (retries already exhausted)" reasoning, all in prose. | Bulletize VPC-1..4. For VPC-2, state the principle ("each party vetted against the *counterparty's* requirements") then the two concrete pairings. Render VPC-4 as a **mapping table** (fail→counterparty; indeterminate/error→permanent; EXCEPT sole-cause unparseable-counterparty-presentation→counterparty §7.8.2), then two callouts: (a) errorClass = blame only, decision unchanged; (b) no `transient` at phase level — retries exhausted by this point (VP-R4), matches §7.8.2 cause table. Verbatim. | fixed |

### DACS-2 systematic-sweep additions (≥110-word sweep, same patterns the read-through missed)

| # | Location | Category | Issue | Fix | Status |
|---|----------|----------|-------|-----|--------|
| 22 | DACS-2 §7.4.3 (RA-1..RA-5 + verifier resolution) | SC | Inline `(RA-1)…(RA-5)` rule list + the "A verifier MUST resolve a recipe by: …;…;…" semicolon-procedure, both in running prose | RA-1..5 → bullets (one per rule, `"dacs-recipe:v1:"` separator verbatim); the resolution sequence → numbered 1–4 list. | fixed |
| 23 | DACS-2 §7.4.5 (RAV-1..RAV-7) | SC | Inline `(RAV-1)…(RAV-4)` consumer-obligations + `(RAV-5)…(RAV-7)` steward-obligations rule lists in prose | Two bulleted lists, one rule per bullet, verbatim (incl. RAV-3 `mocked`→error + operator_gated/closed_data/bilateral carve-out). | fixed |
| 24 | DACS-2 §7.7 (WN-1..WN-6) | SC | Inline `(WN-1)…(WN-6)` warning-rule list in one running sentence | Bullet list, one WN rule per bullet; keep the §7.6.1 retry-taxonomy alignment sentence after. | fixed |
| 25 | DACS-2 §7.5.2 (attestation resolution algorithm) | SC | One semicolon-chained "MUST validate by: fetching…; checking…; for…; for…; for methods with signer…; optionally…" procedure | Numbered 1–4 steps; the two integrity branches (canonical-JSON vs raw-byte) as a nested sub-list under step 2; both "mismatch MUST cause rejection" MUSTs verbatim. | fixed |
| 26 | DACS-2 §7.6 (verification procedure (1)–(8)) | SC | Inline numbered `(1)…(8)` procedure flattened into one sentence | Real numbered 1–8 list; "(failure to match MUST set decision = fail)" verbatim. | fixed |

## Post-pass review — DACS-2 (two independent cold reviewers, 2026-06-07)

Two fresh-eyes reviewers ran in parallel: (A) meaning-preservation, diffing every hunk old-vs-new; (B) cold internal-consistency read of the whole module.

- **Reviewer A: CLEAN (0 critical).** Confirmed every restructured hunk behavior-preserving — no dropped/weakened/strengthened MUST across all rule families; PSP-2 and VPC-4 tables faithfully encode the prose mappings (polarity, pass/fail/error/indeterminate, the counterparty-malformed carve-out); the **#14 oauth→§7.3.2 reference is not dangling** and oauth retains full force (nonce-include MUST + reject-on-mismatch MUST + document-residual MUST); all verbatim values/addresses/separators/formulas intact. Two cosmetic minors: the §7.7.2 `signed_bytes` line rendering (**fixed** — blank line added) and the VP-C3 explanatory gloss (kept — accurate, adds no obligation).
- **Reviewer B: 1 "critical" + 3 minor — all triaged as pre-existing or false-positive, none introduced by this pass.**
  - **"GOV-1 undefined" — FALSE POSITIVE (single-module scope).** GOV-1 *is* defined in **CORE.md §14.7** (steward disclosure); GOV-2/GOV-3 in DACS-2 continue the cross-module family. The diff confirms this pass did not touch GOV numbering. No action.
  - **§7.9 conformance summary omits PSP/WN/GOV/PA families — pre-existing**, untouched by this pass (the §7.9 table maps roles→rule-ranges and predates it). Flag for steward as a possible completeness cleanup; out of scope for a behavior-preserving readability pass.
  - **§7.5.1(a) "MAY retry per recipe.retryClass" loose wording** + **§7.5.1(b)/VP-R4 indeterminate-rationale restatement** — both pre-existing summary-vs-rule wordings, not introduced here. Left as-is.

**Convergence:** the only review action item (the §7.7.2 rendering) is fixed; no normative meaning changed. DACS-2 treated as clean. Pre-existing items (§7.9 family coverage) noted for a future steward cleanup, not this pass.

## DACS-3 (Negotiate) — pass complete 2026-06-08

Read-through finds #27–#32 fixed, THEN a systematic ≥110-word sweep added #33–#37 (the read-through missed more of the **same** inline-rule-list / procedure-wall pattern — RFQ procedure + RFQ-1..4, SE-1..7, CA-1..4, PS-1..3 conformance lists, the selection-rules wall, and the §8.3.4 channel-failure conditions). 25/25 rule IDs + 120/120 MUSTs (13 MUST NOT, by *occurrence*) preserved throughout; 5 validators + 35 tests green. Two cold fresh-eyes reviews ran (see below).

**Method note (gotcha):** verify MUST/MUST NOT as **occurrences** (`grep -oE … | wc -l`), NOT lines (`grep -c`). Bulletizing a multi-MUST-NOT sentence spreads the keywords across more lines, so a line-count *looks* like it grew (11→12) while the true occurrence count is unchanged (13→13). The §8.5.2 ordering note already existed as a standalone paragraph, so #31/#32 *reference* it rather than restating — no introduced duplication.

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 27 | DACS-3 §8.5–§8.6 (auto-accept commitment, orchestrator-time procedure) | SC | A numbered `(1)…(4)` orchestrator procedure flattened into one prose sentence, where **step (1) balloons into a wall** fusing the two-phase `validUntil` check (provisional vs current clock at signing time → already-expired MUST NOT sign; authoritative `committedAt ≤ validUntil` re-check at commit-agreement against the anchored `committedAt`, §8.6, per §8.5.2 ordering) + its rationale (per-session `committedAt` doesn't exist pre-anchor). Step (4) also fuses the instance-signature domain separator + the "NOT pre-issued / MUST be live / delegated auto-signer scope" rule. | Render `(1)–(4)` as a numbered list. Pull step (1)'s validity check into a labelled **two-phase validity** sub-block: *provisional* (current clock at signing time; already-expired MUST NOT produce an instance signature) → *authoritative* (`committedAt ≤ validUntil` at commit-agreement vs the anchored CommitmentRecord §8.6, §8.5.2 ordering), with the "committedAt doesn't exist pre-anchor" as the *why*. In step (4) display the signed payload `"dacs-auto-accept-instance:v1:" || agreementHash || autoAcceptCommitmentHash`, then a callout: the instance signature is **NOT pre-issued** — MUST be produced live by the seller's keyholder or an explicitly-delegated auto-signer; the commitment authorises the auto-signer's *scope*, it is not the acceptance. Keep all MUSTs + the domain separator verbatim. | fixed |

| 28 | DACS-3 §8.x (sealed-envelope commit-reveal auction `Procedure`) | SC | The single densest wall in DACS-3 — a 7-step commit-reveal auction `(1)…(7)` flattened into one paragraph, every step ballooning with inline rules + security rationale. Buried load-bearing rules: the `bidHash` construction (domain tag + pre-hashed bid + salt → unambiguous boundary), identity=authenticated-sender (CH-3, not free-text `bidderClaim`), `commitTimestamp` informational-only (SR-2 anchor time authoritative, SE-2/SE-5), the **anti-suppression** rule (each *bidder* anchors its own reveal; the anchored in-window bidHash-matching set — NOT the orchestrator inbox — is authoritative), the ordered selection exclusions (currency → non-positive §9.3 → reserve floor/ceiling, CD-1, inclusive bound), the `selectionRule` content-hash-binding (§6.3.4, PricingSpec copy MUST equal it), and the tie-break ladder (earliest SR-2 commit-anchor time → ascending lex `bidHash`). | Render `(1)–(7)` as a numbered list. Commit step → sub-bullets + a **bidHash construction** note (formula `sha256("dacs-sealed-bid:v1:" || sha256(canonical_JCS(bid)) || salt)` as a display line, verbatim; the pre-hash-boundary + domain-tag + commitTimestamp-not-in-preimage rationale beside it) + the identity=CH-3-sender rule + the commitTimestamp-informational rule. Reveal step → sub-bullets + an **anti-suppression** callout (bidder anchors own reveal; anchored-in-window set is authoritative). Selection step → an *ordered* exclusion sub-list (currency → non-positive → reserve with floor/ceiling per rule + CD-1 + inclusive `==`), then selectionRule (content-hash-bound, must-equal), then a **tie-break ladder** sub-list. No-winner step → the errorClass mapping (counterparty vs permanent). Keep every MUST, the `dacs-sealed-bid:v1:` separator, the hash formula, CD-1, and all §/SE refs verbatim. (Largest single SC item so far — high care on MUST/separator preservation.) | fixed |

| 29 | DACS-3 §8.5.1 (canonical serialisation + Rule CD-1 + required signers) | SC | The **CD-1** rule fuses its rationale (JCS canonicalises JSON *numbers* but preserves *string* bytes verbatim, and amounts are strings) + the 5 formatting constraints (buried mid-sentence) + producer/verifier timing obligations + the "1.50 vs 1.5" consequence, all in prose. The required-signers-per-pattern list is also inline. | Lead with canonical form (JCS minus signatures) → agreement hash `sha256(canonical_form)` hex → signature over `"dacs-agreement:v1:" || agreement_hash` (separator verbatim). Pull **Rule CD-1** into its own block: one-line *why*, then the 5 constraints as a sub-list (no leading zeros except single 0 before point / no trailing zeros / `.` only separator / no `+` / no exponent), then producer (canonicalise before any hash) + verifier (canonicalise before §8.5.2 comparisons) obligations, then the "1.50 ≡ 1.5 → identical bytes/hash/sig" consequence. Render **required signers** as a small table (fixed-price → buyer+seller [seller may be auto-accept instance sig §8.4.1]; rfq → buyer+seller; sealed-envelope → seller+winning bidder). Keep all MUSTs verbatim. **NB: §8.5.1 is the CD-1 *home* flagged for the §10 Core hoist — restructure in place now; the move to CORE is the separate v0.2 step.** | fixed |
| 30 | DACS-3 §8.5.1 (`priceAnchor` canonical-form note) | SC | Braids four separable rules into one paragraph: priceAnchor-is-in-the-signed-canonical-form when present; `priceAnchor.price` MUST be CD-1; unknown-field + absence tolerance (MUST NOT reject on either); conditional audit validation (if used, MUST resolve `attestationRef` to a valid SR-3 attestation + `contentHash == sha256(raw body)`). | Rule-first: priceAnchor is **optional / informational** (non-normative for agreement validity). Then bullets: (a) when present it is in the JCS canonical form and covered by both signatures, and its `price` MUST be CD-1; (b) **tolerance** — a verifier that doesn't understand it MUST NOT reject, and its absence MUST NOT cause rejection; (c) **audit use (conditional)** — *if* a consumer uses it, it MUST resolve `attestationRef` to a valid SR-3 attestation and confirm `contentHash == sha256(raw response body)`. Verbatim MUSTs. | fixed |

| 31 | DACS-3 §8.5.2 (listing conformance validation) | SC | Seven independent agreement-vs-listing MUST checks (currency / price-band / rail / deliverable / deadline / not-expired / pattern) strung into **one sentence**, with check #2 (price band) ballooning into a formula + a half-up rounding rule + its rationale ("not currency precision" — undefined at listing time, settlement precision is `rail.asset.decimals`) + the lower-bound>0 guard + the fixed-price-over-negotiable exact-equality special case (PS-3), all inline. | Render as a numbered **checklist** of the 7 checks (reject on any failure; "rejected by commit-agreement" framing kept). Lead check #1 with "reject cross-currency before any amount comparison." Make check #2 a sub-block: the band interval `[bandCenter.amount×(100−minPct)/100, bandCenter.amount×(100+maxPct)/100]` as a display line (verbatim), then the rounding rule (half-up to `bandCenter.amount`'s CD-1 fractional digits, with the "not currency precision / settlement precision = rail.asset.decimals" as a *note*), the inclusive full-precision comparison, the lower-bound≤0 reject, and the **PS-3 fixed-price-over-negotiable → equal bandCenter exactly** special case (+ fixed-pricing → listed price). For #5/#6 keep the `committedAt` = SR-2-anchor-time (not self-reported `generatedAt`) anti-backdating note and the read-vs-commit (§6.3.4 step-3) re-check rationale. Keep every MUST, formula, and § ref verbatim. (Second-largest SC item after #28.) | fixed |

| 32 | DACS-3 §8.6 (commit-agreement procedure) | SC | Numbered `(1)…(4)` orchestrator procedure flattened into prose; step (3) carries the §8.5.2 ordering subtlety inline — value checks (currency/band/rail/deliverable/pattern) gate *here* (pre-anchor), but the two `committedAt`-relative checks (deadline, `notAfter`) are re-evaluated *after* step 5 against the anchored `committedAt` (same committedAt-doesn't-exist-pre-anchor reason as #27) + the failure→`permanent` outcome. | Render `(1)–(4…)` as a numbered list. Split step (3) into two labelled groups: **value checks** (gate here, pre-anchor) vs **committedAt-relative checks** (deadline, `notAfter` — deferred to post-anchor, step 5), with the one-line *why* (`committedAt` = SR-2 anchor time, doesn't exist until anchored) cross-ref to the §8.5.2 ordering note; keep "any validation failure MUST cause the phase to fail with class `permanent`". Verbatim hash formula + MUSTs. | fixed |

### DACS-3 systematic-sweep additions (≥110-word sweep, same patterns the read-through missed)

| # | Location | Category | Issue | Fix | Status |
|---|----------|----------|-------|-----|--------|
| 33 | DACS-3 §8.4.2 (rfq Procedure (1)-(7) + RFQ-1..4) | SC | Inline `(1)…(7)` procedure + inline `(RFQ-1)…(RFQ-4)` conformance, both in running prose | Procedure → numbered 1–7 list; RFQ-1..4 → bullets; "Consent MUST be explicit" + Substrate line kept. | fixed |
| 34 | DACS-3 §8.4.3 (selection-rules wall + SE-1..7) | SC | 532-word "Selection rules and the rule-ref binding requirement" wall (4 rule types + rule-ref content-hash sub-procedure) + inline `(SE-1)…(SE-7)` conformance | Selection rules → bullet per rule type (lowest/highest · first-acceptable · rule-ref, with the fetch+hash-verify steps); SE-1..7 → bullets; `errorClass: permanent` + salt requirements verbatim. | fixed |
| 35 | DACS-3 §8.6 (CA-1..4 conformance) | SC | Inline `(CA-1)…(CA-4)` rule list | Bullet list, one CA rule per bullet, verbatim. | fixed |
| 36 | DACS-3 §8.8 (PS-1..3 validation) | SC | Inline `(PS-1)…(PS-3)` rule list | Bullet list; fixed the `negotiate-*phase` missing-space typo → `negotiate-* phase`. | fixed |
| 37 | DACS-3 §8.3.4 (channel-failure conditions) | SC | "A member MUST treat the channel as failed when: a; b; c" — three conditions flattened into one sentence | Lead sentence + 3-bullet list of the conditions; the single MUST preserved. | fixed |

## Post-pass review — DACS-3 (two independent cold reviewers, 2026-06-08)

- **Reviewer A (meaning-preservation): CLEAN (0 critical, 0 minor).** Diffed every hunk; confirmed MUST 120→120, MUST NOT 13→13, SHOULD 12→12, MAY 20→20 with no MUST↔MUST-NOT conversions; the required-signers table, the §8.5.2 7-check list, the sealed-envelope 7-step procedure, the selection-rules bullets, and the reserve floor/ceiling + inclusive-`==` + tie-break ladder all faithful; bidHash formula present in both commit + reveal; all domain separators, the band formula, CD-1's 5 constraints, SE-7 salt requirements, and the #27/#32 two-phase committedAt split intact.
- **Reviewer B (cold consistency/structure): 0 critical, 4 minor (all cosmetic markdown).** No contradictions, no dangling intra-module refs, rule-ID hygiene + §8.9 conformance summary clean. Minor markdown findings triaged:
  - **Four list-item→bold-heading boundaries with no blank line** (§8.4.3 step 7→"Sealed bid body schema"; selection-rules→"Conformance"; §8.6 step 6→"Conformance"; PS-3→"Fallback") — **introduced by this pass; all four FIXED** (blank line inserted so the heading renders as its own paragraph).
  - **§8.6 ordered list split by the `CommitmentRecord` code block** (items 1–4, code block, then 5–6 restarting at `5.`) — GFM honours the `5.` start; the original was inline-numbered across the same code block, so no regression. Left as-is (the folding of "Conformance" into item 6 was the real bug — fixed above).
  - **CH-6 restated in §8.12** and **two §8.4.1 paragraph→bold boundaries** — **pre-existing**, not introduced by this pass; left out of scope (CH-6's §8.12 restatement is intentional security-narrative emphasis).

**Convergence:** meaning-preservation clean; the only introduced defects (4 blank-line boundaries) are fixed; re-gate green. DACS-3 treated as clean.

## DACS-4 (Settle) — pass complete 2026-06-08

Read-through finds #38–#48 (+#46b) fixed, plus systematic-sweep additions for same-pattern walls the read-through missed: **RD-1..5** (§9.4.3) and **RAV-R1..5** (§9.4.4) inline rule-lists → bullets; all six **rail-handler Procedures** (§9.5.2–§9.5.7) → numbered steps and **Failure-modes** → `cause → class` bullets; the **§9.6 delivery procedures** (storage-program, attested-payload) → numbered steps. (HTLC-1..10 were *already* bulleted in the source — #44 was a no-op; §9.4.4 availability values already bulleted.) 39/39 rule IDs + 90/90 MUSTs (21 MUST NOT, by occurrence) preserved; 4/4 domain separators; 5 validators + 35 tests green. Two cold reviews: meaning-preservation **CLEAN**; consistency review found only cosmetic items (below).

**Largest batch so far** (~20 edits on the biggest module). Also normalised 11 pre-existing "intro-sentence → `**Heading.**`" rendering folds (blank-line insertions) that the cold review surfaced. §9.10–§9.13 (conformance summary, rationale, backwards-compat, security) left as already-structured (bold-label / Threat→Mitigation), consistent with DACS-2/3.

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 38 | DACS-4 §9.x (`PricingSpec` `negotiable` variant inline comment, line ~52) | SC | An inline `//` type comment carries a whole semantics paragraph: the band formula + half-up rounding + inclusivity + lower-bound≤0 reject (all **restating §8.5.2**, the authoritative rule) **plus** the load-bearing-but-buried asymmetry rationale (`minPct < 100` keeps the floor positive; `maxPct` intentionally unbounded → NO upper price protection → buyer SHOULD set its own ceiling). The key insight — *the band protects the floor, not the ceiling* — is hidden at the end of a long comment. | Keep the inline comment terse (field meanings + "band per §8.5.2"); **defer the formula/rounding/reject rule to §8.5.2** rather than restating it (avoids drift between the two copies). Pull the **asymmetry note** into short prose after the code block: `minPct < 100` ⇒ positive lower bound; `maxPct` unbounded ⇒ open-ended upside is legitimate, so the band gives **no ceiling** and a buyer-side orchestrator SHOULD enforce its own acceptance ceiling. Keep the MUST (reject if lower bound ≤ 0) wherever it lands, verbatim. | fixed |

| 39 | DACS-4 §9.x (DeliverableRef canonical hash) | SC | Fuses the hash definition (`sha256(JCS(DeliverableSpec))`, same as every artifact) + the why-JCS (discriminated union w/ optional fields → reproducible) + the load-bearing rule (both parties MUST compute over the *anchored* `offering.deliverable`, not a re-derived copy, for §8.5.2 byte-determinism). | Rule-first: state the hash = `sha256(canonical_form)` (defer the generic JCS rule to the canonical-form section rather than re-explaining). Then a **callout**: both parties MUST hash the **anchored** copy (not re-derived), else trivial formatting/optional-field differences make §8.5.2 nondeterministic. JCS-reproducibility as the one-line *why*. | fixed |
| 40 | DACS-4 §9.x (PriceTerm.amount positivity, normative) | SC | Fuses the positivity rule (finite, strictly > 0; never NaN/∞/negative) + the 3-consumer rationale (winner-selection §8.4.3, band §8.5.2, on-chain amount §9.5.2) + the timing (before selectionRule, before commit-agreement) + the specific attack (non-positive excluded, not selected as a lowest-price winner). | Rule-first sentence (amount MUST be finite & > 0). Then a **why/where** block: bullet the 3 consumers; state the two enforcement checkpoints (reject before `selectionRule`, before commit-agreement); and the attack callout — a zero/negative bid would otherwise win a `lowest-price` auction, so a violating revealed bid MUST be excluded from the candidate set. Verbatim MUSTs. | fixed |

| 41 | DACS-4 §9.5.1 (pay-* common contract PC-1..PC-6) | SC | PC-1..PC-6 are line-separated, but **PC-2, PC-4, PC-5 each balloon into a wall**: PC-2 fuses the anchor address + the phaseIndex anti-collision rationale (PIPE-5) + the `:resolved` ST-8 note + the CF-4 encoding (which segments encode); PC-4 fuses a 5-value errorClass enumeration + the HTLC-9 asymmetric sub-case; PC-5 fuses the currency-resolve rule + a 3-way AssetSpec-kind mapping. | PC-2: lead with the address `dacs4:payment:{jobId}:{railId}:{phaseIndex}[:resolved]` as a display line, then bullets (phaseIndex = anti-collision discriminator for repeated pay-* / PIPE-5; `:resolved` for ST-8 supersede; CF-4-encode railId only). PC-4: render the errorClass set as a **table** (class → trigger), then the HTLC-9→settle-asymmetric note. PC-5: render the "resolves" mapping as a small **table** (AssetSpec kind → required `amount.currency` field). Keep every MUST/MUST NOT verbatim. | fixed |
| 42 | DACS-4 §9.5.1 (PC-7 cross-chain anchor/settlement decoupling) | SC | The single deepest wall in Settle — one paragraph fusing the principle (irreversible foreign-chain movement decouples anchoring from settlement success) + a 4-part (a)-(d) obligation list + the ST-7 carve-out framing + two exclusions (HTLC-9 half-settled → settle-asymmetric; retry-must-check-foreign-state to avoid double-settlement). | Lead with the **principle** ("once value has irreversibly moved on the foreign chains, anchoring is bookkeeping that catches up — never a reason to fail/pause"). Render **(a)-(d)** as bullets verbatim (return ok:true w/ txRefs; durable idempotent anchor-retry on the PC-2 address; MAY defer attestationRef; MUST NOT classify `substrate`). Then two **callouts**: the ST-7 carve-out (the only exception to substrate-failure-pause), and the two guards — EXCLUDES HTLC-9 (→ settle-asymmetric/ST-8, never ok:true), and retry MUST re-check foreign-chain state first. Verbatim MUSTs + the `dacs4:payment:…` address. | fixed |

| 43 | DACS-4 §9.5.2–§9.5.7 (all six pay-* rail handlers) | SC | Uniform across every handler (erc20, spl, htlc, liquidity-tank, ap2, x402): the **Procedure.** is a semicolon-chained sequence and the **Failure modes.** is an inline `cause → errorClass` list. §9.5.4 (HTLC) procedure is the heaviest (4-step swap + preimage/hashlock derivation); §9.5.5 (tank) also carries two rule paragraphs (**No mechanism substitution**, **Evidence scope**) worth structuring. | For each handler: **Procedure** → numbered steps; **Failure modes** → a 2-col table (`cause → errorClass`). For §9.5.4 specifically, lead the procedure with the preimage/hashlock derivation block (HTLC-5/HTLC-6 formulas verbatim) then the 4 ordered swap steps (lock-source → lock-dest → payer-claims-dest/reveal → payee-claims-source) + the txRefs note. For §9.5.5 keep No-mechanism-substitution as a rule + Evidence-scope as a status note. Verbatim MUSTs, addresses, formulas, contract addrs. | fixed |
| 44 | DACS-4 §9.5.4 (HTLC-1..HTLC-10 salt/preimage/timelock rule family) | SC | The buyerSalt entropy/lifecycle + preimage-derivation + timelock rules are an inline `(HTLC-1)…(HTLC-10)` rule list interleaved with non-normative **Rationale** paragraphs. | Render HTLC-1..HTLC-10 as a bullet list (one rule per bullet), keeping the HKDF derivation `preimage = HKDF(IKM=buyerSalt, salt=jobId, info=agreementHash)` (HTLC-5) and the HTLC-7 timelock inequality verbatim; keep the interspersed **Rationale (non-normative)** paragraphs as a separate clearly-labelled block (don't merge into the rules). | fixed |
| 45 | DACS-4 §9.5.4 (HTLC-9 asymmetric-settlement evidence) | SC | The densest single wall in §9.5 — one paragraph fusing: why the asymmetric state differs from a timeout (payer already paid on dest) + the MUST-NOT-refund rule + the `(a)/(b)` evidence requirements + the finality gate (don't enter before htlc-reveal finality) + the entire ST-8 `settle-asymmetric` forward-resolution flow (watch until expiry_source → superseding success record w/ supersedesEvidenceRef, or terminal settle-failed) + the DACS-5 weighting note. | Lead with the principle (asymmetric ≠ timeout: payer already received dest value, so source MUST NOT be refunded — double-dip). Then **evidence requirements** as `(a)/(b)` bullets (outcome:"failure" + dest-revealed-source-unclaimed marker; paymentTxRefs include htlc-reveal). Then a **resolution** sub-block: finality gate to enter; ST-8 non-terminal watch until expiry_source; success path (superseding outcome:"success" + supersedesEvidenceRef → resume PIPE-3/4) vs expiry path (terminal settle-failed → failed-counterparty). Verbatim MUSTs + state names. | fixed |

| 46 | DACS-4 §9.8 (amendment validity AMEND-1..AMEND-4) | SC | A rationale lead + the 3 amendment types (refund / partial-refund / correction) + 4 inline rules + a **large ST-8 parenthetical wedged inside AMEND-2** (supersedesEvidenceRef ≠ amendsEvidenceRef; not subject to AMEND-1..4; post-resolution refund references the `:resolved` success record) + the AMEND-3 cross-currency caveat — all braided into one block. | Lead with the principle (an amendment MUST bind to a real, successful settlement) + define the **3 amendment types** (refund/partial-refund carry `refundAmount`; correction MUST NOT). Render AMEND-1..4 as bullets (AMEND-1 same-jobId real evidence; AMEND-2 outcome==success + failed→rail-refund-path-not-amendment; AMEND-3 Σrefund ≤ paymentAmount, currency-matched, same-currency-only in v0.1; AMEND-4 reject-or-flag-never-silent). **Pull the ST-8 aside out of AMEND-2 into its own callout** ("ST-8 supersession is not an amendment": supersedesEvidenceRef not subject to AMEND-1..4; post-resolution refund → the `:resolved` success record, not the superseded interim failure). Verbatim MUSTs + field names. | fixed |

| 47 | DACS-4 §9.9 (pipeline composition PIPE-1..PIPE-5) | SC | Inline `(PIPE-1)…(PIPE-5)` rule list in running prose (PIPE-1 has a long pay-less-pipeline reconciliation aside; PIPE-5 a long fee-split-not-representable aside). The composition *patterns* above (pay-then-deliver / deliver-then-pay / escrow-with-delivery-gate / streamed) are already itemized but could be tightened. | PIPE-1..5 → bullets, one rule per bullet; keep PIPE-1's §6.3.4(8) pay-less reconciliation + PIPE-5's fee-split roadmap note as sub-notes under their bullets. Verbatim MUSTs. | fixed |
| 48 | DACS-4 §9.8 (cross-chain atomic settlement SR-5) | SC | "**Choosing a rail**" compares HTLC vs Liquidity Tank across Cost / Latency / Trust in prose (clear table candidate); "**Atomicity guarantee**" fuses the one MUST + per-mechanism bounds (HTLC timelock / tank epoch / substrate-native) + the HTLC-9 exception. | "Choosing a rail" → a **table** (rail × {cost, latency, trust}) with the SHOULD-declare-trust note beneath. "Atomicity guarantee" → lead MUST + a small per-mechanism bounds list + the HTLC-9 exception (surface as asymmetric-settlement evidence, resolve via settle-asymmetric/ST-8) as its own callout. Leave "messaging vs settlement" as coherent prose. Verbatim. | fixed |
| 46b | DACS-4 §9.7.1 (amendment anchoring) — fold into #46 | SC | The amendment anchor address `dacs4:amendment:{jobId}:{evidenceHash}:{amendmentIndex}` + the `dacs-amendment:v1:` signed-payload sentence sit just above AMEND-1..4. | When fixing #46, keep the anchoring sentence (address + `dacs-amendment:v1:` separator + the bundle-inclusion-if-before-finalisation note) verbatim as the lead of the amendment block. | fixed |

## Post-pass review — DACS-4 (two independent cold reviewers, 2026-06-08)

- **Reviewer A (meaning-preservation): CLEAN (0 critical, 0 meaning-affecting).** Verified keyword counts identical HEAD→working (MUST 90→90, MUST NOT 21→21, SHOULD 11→11, MAY 12→12, REQUIRED 6→6) with the MUST-NOT *predicate set* byte-identical (no MUST↔MUST-NOT conversion); PC-4 errorClass table, PC-5 resolve table, and §9.8 choosing-a-rail table faithful to the prose; all domain separators / addresses / the HKDF + timelock formulas / tank contract addrs / finality defaults intact; PC-7 + HTLC-9 carve-outs fully preserved.
- **Reviewer B (cold consistency/structure): 0 critical, 4 minor — all cosmetic or pre-existing.**
  - **Rendering folds** (intro-sentence → `**Heading.**`, and two §9.5.4 rationale→heading folds): pre-existing, **normalised** (11 blank-line insertions); 0 folds remain.
  - **§9.10 conformance summary omits an Orchestrator (RAV-R1..5) row and doesn't explicitly cite AMEND-1..4** — pre-existing completeness gap (same shape as the DACS-2 §7.9 gap already on the roadmap); **noted for the roadmap conformance-summary item, not fixed in this behavior-preserving pass.**
  - **PC-4's literal `substrate` trigger ("SR-2 unavailable") reads narrower than RAV-R4's "rail non-operational" use of the same class** — pre-existing, reconcilable (both = infrastructure fault, no value moved, neither party at fault); left.
  - **HTLC-9 defined out of numeric sequence** (in its own "Asymmetric-settlement evidence" subsection, not inline in the HTLC-1..10 list) — deliberate, pre-existing; left.

**Convergence:** meaning clean; introduced/cosmetic items fixed; pre-existing gaps noted. DACS-4 treated as clean. Three modules (DACS-1/2/3) + DACS-4 now readability-complete; DACS-5 remains.

## DACS-5 (Verify) — pass complete 2026-06-09

Full pass (read-through + ≥110-word sweep in one go, since this module was unread). Fixed #49–#55: the two intricate state rules **ST-7 / ST-8** (massive single bullets → labelled sub-bullets, **structure-only / verbatim sentences**), **§10.4.3** (canonically-diverge definition → callout + consumer (a)-(d) bullets), **§10.5.1** three reconciliation guards → bullets, **§10.5.3** determinism receipt (1)(2)(3) → bullets, **§10.6.1** rate-phase MUST-list + **§10.7.1** publisher MUST-list → bullets. 12/12 rule IDs + 78/78 MUSTs (12 MUST NOT, by occurrence) preserved; 2/2 domain separators; 5 validators + 35 tests green. Two cold reviews: meaning-preservation **CLEAN** (1 cosmetic non-normative connective drop, no obligation lost), consistency **CLEAN**. The reputation-derivation `derive()` pseudocode + tables (transition table, state→outcome map, conformance summary) were already well-structured; §10.9–§10.11 (rationale / backwards-compat / security) left as bold-label / Threat→Mitigation, consistent with DACS-1..4.

**ST-8 note relocation:** the general "ST-8 does NOT add a per-phase sub-state" note moved from between resolution (a)/(b) to after (b) (with the other closing notes) — a pure move, confirmed meaning-neutral by review. **All of DACS-1..5 now readability-complete.**

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 49 | DACS-5 §10.3.1 (ST-7 substrate-failure pause & resume) | SC | One ~300-word bullet fusing: the pause condition + retry; resume to paused-from state; the `settle-asymmetric` `expiry_source` bound; the time-bound + `failed-substrate`; resume-clears-condition; and the **Precedence over abort** rule. | Keep `- (ST-7)` lead; break into sub-bullets (pause+retry / resume / settle-asymmetric bound / time-bound→failed-substrate / resume-clears / **Precedence over abort** callout). **Structure-only — verbatim sentences**, every MUST kept. | fixed |
| 50 | DACS-5 §10.3.1 (ST-8 cross-chain asymmetric open state & resolution) | SC | The densest state rule — one bullet fusing: open-state definition + entry-finality precondition; interim-evidence anchor + `settle-pending→settle-asymmetric`; the watch-until-`expiry_source`; **Resolution (a)** (resolved→`:resolved` success + supersedesEvidenceRef + resume) incl. SR-2-unavailable pause; the "no per-phase sub-state" note; **Resolution (b)** (expiry→`settle-failed`/`failed-counterparty`). | Keep `- (ST-8)` lead + the open-state definition; then **Resolution (a)/(b)** as labelled sub-bullets, with the SR-2-unavailable-pause and the no-sub-state note as nested notes. Structure-only, verbatim; keep the `:resolved` address + all field/state names. | fixed |
| 51 | DACS-5 §10.4.3 (bundle production: canonically-diverge def + consumer (a)-(d)) | SC | A wall fusing the once-normative **"canonically diverge" definition** + the consumer obligations `(a)/(b)/(c)/(d)` (fetch both / one-present→suppression / both-non-diverge→unified / both-diverge→disputed) flattened into prose. | Pull the "canonically diverge" definition into a labelled **Definition** callout (it's referenced by §10.5.1 guard (ii)); render consumer obligations as an `(a)-(d)` bullet list. Verbatim MUSTs + the two `stor-{sha256(...)}` addresses. | fixed |
| 52 | DACS-5 §10.5.1 ("Two-sided reconciliation", normative) | SC | ~600-word wall — the **three guards (i)/(ii)/(iii)** (signature-validation-first / divergence→exclusion / buyer-seller-only) + failed-substrate denominator + scalar-vs-array null/empty rules + the ratings-binding rules + the volume-resolution rule, all in one paragraph. The most intricate prose in the spec. | Lead with the reconciliation principle; render the **three guards** as a bullet list (verbatim); split the remaining rules into labelled sub-paras (**failed-substrate denominator**, **null vs empty metrics**, **rating binding**, **volume resolution**). Structure-only — do NOT reword any normative sentence; keep every MUST + `party_fault_denom` formula. | fixed |
| 53 | DACS-5 §10.5.3 (determinism receipt, normative) | SC | The receipt fuses requirements `(1)/(2)/(3)` (bundleRefs = reconciled set / canonical ascending-contentHash order / re-run reproducibility) + the windowingBasis-relativity caveat + the not-completeness caveat into one block. | `(1)/(2)/(3)` → bullets; the windowingBasis + completeness caveats as a short note after. Verbatim. | fixed |
| 54 | DACS-5 §10.6.1 (rate phase contract + RT-1/RT-2) | SC | "the phase MUST: run after…; produce…; sign…; anchor…; include…" — an inline MUST-list; RT-1/RT-2 are a separate already-labelled para (light). | Bulletize the phase MUST-list (one obligation per bullet); keep the `dacs-rating:v1:` separator + the `dacs5:rating:{jobId}:{rater}` address (CF-4 escaping note) verbatim. RT-1/RT-2 already structured — leave. | fixed |
| 55 | DACS-5 §10.7.1 (ERC-8004 publisher mapping) | SC | "The publisher MUST: include…; sign…; rate-limit…" — inline MUST-list. | Bulletize (one obligation per bullet). Verbatim. | fixed |

## Reviewed — correct, no change
- **DACS-1 §6.3.1 "Parsers … SHOULD emit lowercase" then CF-2 "Scheme lowercased (MUST)".** Looked contradictory but is intentional scoping (permissive default → MUST at the hash/sign/compare boundary). CF-2 already flags the escalation. No fix; possibly a one-line inline note if it keeps tripping readers.

## Notes
- PL fixes belong to the plain-language pass on abstracts/motivation/rationale.
- SC fixes are structure-only on normative prose — run them through the rule-ID + validator gate (no rule text altered).
- Append new finds as `#N | location | PL/SC | issue | fix | open` while reading.

## SPEC-STYLE pilot — DACS-4 §9.4–§9.5 two-voice rewrite (2026-06-10, spec-style-pilot branch)

A third category beyond PL/SC: **meaning-preserving rewording** (not verbatim) under the new
SPEC-STYLE.md conventions. First applied as a pilot to DACS-4 §9.4–§9.5.

**What changed:** rule text separated from `> **Note (non-normative).**` blocks (rationale,
attack-it-closes, roadmap items, deployment metadata, reference-impl status → Notes); dense
fused paragraphs split rule-per-bullet (PC-2, PC-7, RAV-R4/R5, HTLC-9, progressive anchoring,
no-mechanism-substitution); §9.4.2 maturity bullets → table; PC-2 worked address example added;
txRefs glosses → table. Triage rule: anything with an RFC keyword or interpretation-narrowing
force stayed in rule text (PC-7's ST-7 carve-out and the OPTIONAL-attestationRef clause kept
normative; trust-model MUST kept, only contract addresses demoted).

**Metrics:** prose-lint findings 49 → 5 (residual = benign code-gloss parens in procedure
steps); words 4,798 → ~4,940 (+3%: relocation + structure, not compression — the win is
scanability, not raw length). Rule-ID census identical. Keyword deltas: +1 MUST/+1 MUST NOT
(RAV-R5 explicit split), −1 REQUIRED (HTLC-7 per-param tags merged to "both are REQUIRED").

**Cold adversarial review (independent agent):** 0 critical / 2 major / 2 minor. Majors, both
fixed: (1) missing blank line after 3 Note blockquotes — CommonMark lazy continuation rendered
following *normative* headers inside the non-normative callouts; (2) rewrite invented
"`pay-cross-chain-htlc` is fully supported" (claim not in source) → reworded to "has no v0.1
route restriction". Minors: x402 maturity-table label restored to OLD's taxonomy; **pre-existing**
§9.4.1 `railId` "lowercase ASCII" vs registry `…:USDC` uppercase tension, now instantiated in the
PC-2 example — follow-up issue candidate, not a rewrite defect.

**Lessons for the module sweep:** (a) always blank-line after a Note block (lazy continuation
is a normativity-boundary hazard — consider a lint check); (b) summarising prose into a tighter
claim ("fully supported") is the prose variant of the C1 derived-table failure mode; (c) word
count goes *up* slightly — set expectations on scanability metrics, not length.
