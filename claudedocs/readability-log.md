# DACS Readability / Clarity Log

Running list of wording/structure issues found while reading the spec, to review and fix in one pass at the end. **No normative meaning changes** — these are *how it reads*, not *what it says*.

Two categories:
- **PL — plain-language**: reader-facing prose (Primer, module Abstracts/Motivation/Rationale). Fix = simpler words.
- **SC — structural-clarity**: dense *normative* text where precision is load-bearing. Fix = better structure (define-once + bullets), wording stays exact.

Status: `open` → `fixed`.

**DACS-1 (Identify): pass complete 2026-06-06** — first 7 items (read-through finds) fixed, THEN a comprehensive whole-module sweep added #8–#11 and caught a self-inflicted duplication. 27/27 rule IDs + 103/103 MUSTs preserved throughout, 5 validators + 35 tests green.

**Comprehensive sweep (2026-06-06):** assessed all 14 prose paragraphs ≥110 words. Fixed #8–#11 below. **Corrected a duplication SC#7 introduced** — my CF-4 table came with a "**The rule.**" sentence that duplicated the pre-existing formal **Rule CF-4** (§6.3.4 L422); removed it, folded the MUST/MUST-NOT into the table lead-in. The other ~8 dense paragraphs are single-topic/coherent or already-structured (session-nonce binding, selective-disclosure scope note, SIWD-preferred, sr1-root, revocation, §6.6 security threats, the write-input-mapping (a)(b)(c) block, the formal Rule CF-4 with its worked example, the CF-2 (a)(b)(c) bullet) — left as-is (not the fused-rule+enumeration anti-pattern).

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 1 | DACS-1 §6.1 (abstract, "identity claim reference scheme" bullet) | PL | "a typed reference to an external identifier … optionally paired with a DACS-2 verification method" — jargon-dense for an abstract | "A way to **name an identity that already exists somewhere else** (a domain, DID, company LEI, Google account, signing key), written as `type:value` (e.g. `lei:5493…`). Each name can optionally carry **proof it was checked** against that source." | fixed |
| 2 | DACS-1 §6.1 (abstract, listing bullet) | PL | "The listing is the canonical contract for any transaction against the seller." — "canonical contract" unexplained | "The listing is the seller's **signed, pinned statement of terms** — the single source of truth every deal with that seller is checked against." | fixed |
| 3 | DACS-1 §6.3.1 (CF-2/CF-3, "A ClaimReference has a canonical byte form — what is embedded …") | SC | "what is embedded" / "what is compared" reads like a question mid-sentence | "a *canonical byte form* — **the bytes embedded** whenever the reference appears in a hashed or signed document … — and a *canonical identity* — **the value compared** for matching, reputation keying, and the §7.3.2 replay defence." | fixed |
| 4 | DACS-1 §6.3.2 (PresentationSignature signed payload paragraph) | SC | One wall of text mixing one idea + four byte-exact rules; very hard to parse | Define `signed_bytes` once up front, then one bullet per kind (per-claim / session-key / sr1-root / siwd). Keep every byte-exact rule verbatim. Draft in chat 2026-06-05. | fixed |
| 5 | DACS-1 §6.3.2 (4-tier ranking, inline in the presentedBy sentence) + §6.3.2.1 (3-tier collapse) + §6.3.1 (per-scheme hints) | SC | Claim tiers have **no single citable definition** — the 4-level ranking is buried mid-sentence in §6.3.2, the 3-level IT collapse is restated in §6.3.2.1, scheme hints are scattered in §6.3.1. Reader must reconcile across 3 places, and the 4-tier vs 3-tier views can drift (§6.3.2.1 manually asserts "this is the collapse of §6.3.2"). | Extract a **define-once "Claim tiers" table** (4 levels → schemes → 3-level IT collapse) that §6.3.2 and §6.3.2.1 both *reference* instead of restating. Table drafted in chat 2026-06-05: `1 Authority-issued (lei/finra-crd/sam-uei/fedramp/cmmc/naics)→institutional · 2 DID/ERC-8004+proof→verified · 3 Platform→verified · 4 Plain key→self-declared`. | fixed |

| 6 | DACS-1 §6.3.2 ("For a BundleClaim with verifiedBy present: …" + the freshness-window paragraph) | SC | One dense paragraph fuses *two* things: the 5-step verification checklist **and** the freshness-window formula (with clamping + fail-closed). The inline `min(…)` and the "ignore later wrapper timestamps" clamp are very hard to parse in prose. | Split into (1) a numbered **verification steps** list (fetch → hash-check → parse+recipe → identifier-match → decision==pass → else unverified), and (2) a **Freshness window** sub-block: issuance = `verifiedAt`; `expiry = min(expiresAt ?? ∞, validUntil ?? verifiedAt+defaultMaxAgeSec×1000)`; presenter timestamps clamp (narrow-only, never widen); `defaultMaxAgeSec` from the result's own `recipeVersion` (not latest); fail-closed when undeterminable. Keep the formula verbatim. | fixed |

| 7 | DACS-1 §6.3.4 (CF-4 logical-address encoding paragraph) | SC | One dense paragraph carries: what CF-4 is/isn't + the full list of 7 address kinds it applies to + which segment is variable in each + the why. The per-address variable-vs-fixed detail is impossible to scan in prose. | Lead with the one rule (encode internal `:` of variable segments → `%3A`; leave structural delimiters), then a **table** (address → variable segment(s) → fixed segments), then the one-line why (undecidable boundaries → divergent native addresses). Note CF-4 governs the *name's* parseability, not the native-address formula. Keep all address patterns verbatim. Table drafted in chat 2026-06-06. | fixed |

| 8 | DACS-1 §6.3.2 (presentedBy selection paragraph, 206w) | SC | Fuses the selection rule + reader-acceptance + the big "Reputation MUST NOT be keyed against an unverified presentedBy" anti-laundering rule in one 206-word block | Split: selection-rule paragraph + a separate **Verified-presentedBy for reputation** rule. | fixed |
| 9 | DACS-1 §6.3.4 (listing validation order) | SC | "validate in the following order, halting on first failure: (1)…(9)" — an ordered sequence flattened into prose | Numbered list (1–9), each step verbatim. | fixed |
| 10 | DACS-1 §6.3.2 (canonical serialisation paragraph) | SC | General canonical-form/hash rule fused with the SIWD-specific verifier re-derivation MUST | Split: general rule + a separate **SIWD bundle-binding check** block. | fixed |
| 11 | DACS-1 §6.3.2 (BP-1..BP-4 / BR-1..BR-5 conformance) | SC | Inline `(BP-1) … (BP-4)` and `(BR-1) … (BR-5)` lists in running prose | Bullet lists, one rule per bullet, verbatim. | fixed |

## Post-pass review (fresh-eyes, 2026-06-06)
A read-only critical review of the edited DACS-1 (independent reviewer) found **two contradictions the readability pass introduced/hardened** — both fixed:
- **C1 (critical).** The new **Claim tiers** table carried an `identityTier` column mapping `key` → `self-declared`, but the §6.3.2.1 derivation (step 2) maps a *verified* `key:` → `verified`. They are **different axes** (scheme-strength for `presentedBy` vs verification-status for `identityTier`) that SC#5 wrongly merged (a latent pre-restructure tension the table hardened into an authoritative contradiction). Fix: dropped the `identityTier` column from the tiers table; reworded the governance line + the §6.3.2.1 sentence to state the two axes distinctly (verified `key:` → `verified`).
- **C2 (minor).** Rule CF-4 prose listed `listingId` as MUST-percent-encode, but the new CF-4 table (and worked example) treat it as fixed. `listingId` is URL-safe ASCII (§6.3.4) so carries no reserved delimiters — fixed the prose to match the table.

Everything else the restructure touched verified behavior-preserving (signed-bytes bullets, BP/BR, validation list, verify+freshness, CF-4 table all sound; tables well-formed; no dropped MUSTs). Lesson: **a readability table that adds a *derived* column can encode a contradiction the source prose only had latently — always cross-check new tables against the surviving derivation rules.**

## Post-pass review — round 2 (independent, cold, 2026-06-06)
A second fresh-eyes reviewer (no knowledge of C1/C2) read DACS-1 cold. **Clean on criticals** — explicitly confirmed the C1/C2 fixes hold (verified-`key:` → `verified` consistent in all 3 places; CF-4 `listingId` consistent across prose/table/example; no false "cannot disagree" claim; no dropped MUSTs; rule families single-defined; tables well-formed). Two **minor** items fixed:
- §6.4 rationale listed **"OFAC"** among shipped DACS-1 schemes — but OFAC is a DACS-2 sanctions *check*, not an identity scheme (**pre-existing error from the monolith**, count 1 on main). Corrected to the real registry (LEI, FINRA-CRD, SAM-UEI, FedRAMP, NAICS, CMMC).
- `BundleClaim.expiresAt` schema comment "when verifiedBy becomes stale" overstated the field (it only *narrows* the authority window) — aligned to §6.3.2.
- (no-op) two `signed_bytes :=` definitions share the name but are disambiguated by their domain prefixes — cosmetic, left.

**Convergence:** round 2 found no new criticals → DACS-1 treated as clean.

## DACS-2 (Vet) — pass complete 2026-06-07

Read-through finds #12–#21 fixed, THEN a systematic ≥110-word sweep added #22–#26 (the read-through missed five more instances of the **same** inline-rule-list / semicolon-procedure patterns — RA, RAV, WN, §7.5.2, §7.6 — exactly the DACS-1 lesson). 45/45 rule IDs + 97/97 MUSTs (26 MUST NOT) preserved throughout; 5 validators + 35 tests green. Two cold fresh-eyes reviews ran (see below) — no introduced defects.

**#14 was rescoped during application (important catch).** The log proposed hoisting the session-nonce binding into a blanket common-contract rule (e.g. CM-6) applying to *every* method. On re-reading, only **VC (§7.3.2) and oauth (§7.3.6)** carry it — and they *should*, because the other six methods (proxy/evm-rpc/tlsnotary/zktls/domain-tls/self-signed) have no replayable counterparty-supplied attestation. A blanket rule would have been a **normative expansion**, not behavior-preserving, and would have changed the rule-ID set. Instead: VC is the canonical statement (new *Session-nonce binding* sub-block), and oauth **references** it rather than restating the §6.3.2 disjunction. No new rule ID; the dedup *reduced* duplication (the disjunction phrase now appears once).

| # | Location | Category | Issue | Proposed fix | Status |
|---|----------|----------|-------|--------------|--------|
| 12 | DACS-2 §7.3.1 (method common contract, CM-1..CM-5) | SC | Inline `(CM-1) … (CM-5)` list in one running sentence — same hard-to-scan pattern as DACS-1 BP/BR | Bullet list, one CM rule per bullet; keep the CM-2 address `dacs2:{jobId}:{scheme}:{identifier}:v{recipeVersion}` + CF-4 note verbatim. | fixed |
| 13 | DACS-2 §7.3.2 (verifiable-credential `Procedure`, 278w) | SC | One semicolon-chained sentence fusing: the verification step sequence + the holder-binding security rule (incl. the §6.3.2 nonce disjunction + its rationale) + the 4-way pass/fail/error/indeterminate outcome mapping + trust-model/substrate footer | Numbered **verification steps** list; pull holder-binding into its own labelled sub-block (the MUST + nonce disjunction + the replay rationale); render the outcome mapping as a small pass/fail/error/indeterminate list. Keep all MUSTs verbatim. **NB: all 8 method subsections §7.3.2–7.3.9 are "Procedure." prose of this shape — assess each in the DACS-2 sweep.** | fixed |

| 14 | DACS-2 §7.3.2–7.3.9 (every method `Procedure` restates the session-nonce binding) | SC (define-once) | The session-nonce anti-replay binding (the §6.3.2 disjunction: SIWD Nonce else `bundleToVet.sessionNonce`, reject on mismatch) is **repeated in each method procedure** — VC calls it "holder-binding" (§7.3.2), oauth-attested restates it (§7.3.6 "consistent with §7.3.2"), etc. "Describe it N times" at the method level. | **Define once** in the common contract — add a CM rule (e.g. CM-6 "every method MUST bind its attestation to the session nonce per §6.3.2") — and have each method `Procedure` reference it + state only its method-specific caveat (e.g. oauth's "if the service can't carry a nonce, document residual replay risk"). Bigger dedup than bulletizing each procedure (which is #13). | fixed |

| 15 | DACS-2 §7.3.x (PSP-2 "Decision mapping") | SC | A 2-axis decision table (parse-result × predicate-match, with the positive/negative-match polarity inversion via `negativeMatch: true`) **flattened into prose**, plus the `indeterminateOn`-runs-first ordering rule and the per-format predicate-kind list, all in one block. The polarity inversion (match=pass for `lei` vs match=fail for `ofac-clear`) is the load-bearing concept and is buried mid-sentence. | Lead with the polarity concept (positive vs negative match, `negativeMatch:true` flips it). Render the mapping as a **table** (rows: parses+match / parses+no-match / doesn't-parse / authority-inconclusive; columns: positive scheme, negative scheme → pass/fail/error/indeterminate). State the `indeterminateOn`-before-match ordering as a one-line rule above it. Keep predicate-kind list (jsonPath/selector/xPath/matcher) as a small bullet. Values verbatim. | fixed |
| 16 | DACS-2 §7.3.x (PSP-5 "Negative-match completeness floor") | SC | One dense block fusing the rule (MUST confirm completeness before a negative `pass`) + the three accepted completeness signals + the MUST-NOT/→indeterminate + the worked rationale (truncated SDN clears a sanctioned party) + the scope line. | Rule-first sentence ("for a negative-match scheme deciding by *absence* in a downloaded list, MUST prove the download complete before `pass`"); bullet the three completeness signals (declared count / end-of-list sentinel / Content-Length==bytes); separate **Why** callout (can't prove a negative over a partial list; truncation would clear a sanctioned entity); one-line scope (`negativeMatch:true` + "identifier not found in downloaded list"). Verbatim. | fixed |

| 17 | DACS-2 §7.4.x (Progressive anchoring phases PA-1/2/3 + GOV-2/GOV-3) | SC | One block fuses: 3 anchoring phases + the append-only re-anchoring invariant + the pin-time validation rule (pinned recipeVersion validates against the phase/key in force *at pin time*) + GOV-2/GOV-3. Also `anchoring: "in-code"` is **used in the deprecation sentence before PA-1 defines it** — caused real reader confusion ("what are in-code?"). | Define the three `anchoring` values **up front in a small table** (in-code / single-signer / multisig → what it means → trust level → which is current v0.1), before the deprecation sentence uses the value. Then PA-1/2/3 as the phase rules, the **append-only + pin-time invariant** as its own callout (immutable old versions, re-anchor only as new versions, pinned result validates against pin-time phase/key per §7.12/§12.4), GOV-2/GOV-3 as two bullets noting GOV-3 is satisfied by `governance.anchoring` (§7.4.1). Verbatim values. | fixed |

| 18 | DACS-2 §7.6.1 (retry policy VP-R1..VP-R4) | SC | A small **decision matrix** (outcome × retryClass → retry?) written as four prose rules; the unifying principle ("only retry your-side transient failures; an authority's substantive answer is final") is implicit. | Lead with the principle, then a **table**: rows error+transient / error+permanent / indeterminate / fail / pass → retry? + budget. Keep VP-R2 (new attestation) and VP-R4's `retryOnIndeterminate` escape-hatch (pending→conclusive) as riders. Values verbatim (default 3, exp backoff; retryOnIndeterminate default false). | fixed |
| 19 | DACS-2 §7.6.1 (reuse/caching VP-C1..VP-C3) | SC | VP-C1 fuses the reuse-window formula + the "validUntil wins, defaultMaxAgeSec is fallback (from the pinned recipeVersion)" gloss + the §6.3.2 consistency cross-reference + the no-analogue-clamp parenthetical, all in one sentence. | Lead with the formula `now ≤ validUntil ?? (verifiedAt + defaultMaxAgeSec×1000)` as a display block + one-line gloss (validUntil wins; fallback from *that* recipeVersion). Move the §6.3.2-agreement + clamp-has-no-analogue into a **consistency note** callout. Keep VP-C2 (record reference) + VP-C3 (listing `ClaimRequirement.maxAge` can tighten, never bypass) as two bullets. Verbatim. | fixed |

| 20 | DACS-2 §7.7.1 (composite-record anchoring/signature) | SC | One sentence carries three independent MUSTs (anchor via SR-2 / record anchor-ref in DACS-5 session record / sign by verifier over §B.7 payload) **plus** the CF-4 encoding requirement for `{evaluatedParty}` nested in a parenthetical inside the address clause + an inline "verifier (= party running Vet)" gloss. | Split into three short rules: (1) **Anchor** — address `dacs2:composite:{jobId}:{evaluatedParty}` as a display block, with the CF-4 note beneath (`{evaluatedParty}` is a ClaimReference/CF-4 variable segment → percent-encode before assembly, §6.3.4); "or substrate equivalent" kept. (2) **Record** anchor reference in the DACS-5 session record. (3) **Sign** — verifier (party running Vet), §B.7 domain-separated payload. Verbatim. NB: same `dacs2:`+CF-4-variable-segment shape as CM-2 (#14/§10 CF-4-to-Core theme). | fixed |

| 21 | DACS-2 §7.8.2 (orchestrator vet-phase contract VPC-1..VPC-4) | SC | Inline `(VPC-1)…(VPC-4)` list; **VPC-2's cross-check** (buyer-bundle vs listing-side-reqs-on-buyers / seller-bundle vs buyer-side-reqs-on-sellers) is hard to follow; **VPC-4** fuses a 3-row fault-attribution mapping + a big EXCEPT clause (unparseable counterparty presentation → counterparty) + the "fault-attribution-only" note + the "why no transient class (retries already exhausted)" reasoning, all in prose. | Bulletize VPC-1..4. For VPC-2, state the principle ("each party vetted against the *counterparty's* requirements") then the two concrete pairings. Render VPC-4 as a **mapping table** (fail→counterparty; indeterminate/error→permanent; EXCEPT sole-cause unparseable-counterparty-presentation→counterparty §7.8.2), then two callouts: (a) errorClass = blame only, decision unchanged; (b) no `transient` at phase level — retries exhausted by this point (VP-R4), matches §7.8.2 cause table. Verbatim. | fixed |

### DACS-2 systematic-sweep additions (≥110-word sweep, same patterns the read-through missed)

| # | Location | Category | Issue | Fix | Status |
|---|----------|----------|-------|-----|--------|
| 22 | DACS-2 §7.4.3 (RA-1..RA-5 + verifier resolution) | SC | Inline `(RA-1)…(RA-5)` rule list + the "A verifier MUST resolve a recipe by: …;…;…" semicolon-procedure, both in running prose | RA-1..5 → bullets (one per rule, `"dacs-recipe:v1:"` separator verbatim); the resolution sequence → numbered 1–4 list. | fixed |
| 23 | DACS-2 §7.4.5 (RAV-1..RAV-7) | SC | Inline `(RAV-1)…(RAV-4)` consumer-obligations + `(RAV-5)…(RAV-7)` steward-obligations rule lists in prose | Two bulleted lists, one rule per bullet, verbatim (incl. RAV-3 `mocked`→error + operator_gated/closed_data/bilateral carve-out). | fixed |
| 24 | DACS-2 §7.7 (WN-1..WN-6) | SC | Inline `(WN-1)…(WN-6)` warning-rule list in one running sentence | Bullet list, one WN rule per bullet; keep the §7.6.1 retry-taxonomy alignment sentence after. | fixed |
| 25 | DACS-2 §7.5.2 (attestation resolution algorithm) | SC | One semicolon-chained "MUST validate by: fetching…; checking…; for…; for…; for methods with signer…; optionally…" procedure | Numbered 1–4 steps; the two integrity branches (canonical-JSON vs raw-byte) as a nested sub-list under step 2; both "mismatch MUST cause rejection" MUSTs verbatim. | fixed |
| 26 | DACS-2 §7.6 (verification procedure (1)–(8)) | SC | Inline numbered `(1)…(8)` procedure flattened into one sentence | Real numbered 1–8 list; "(failure to match MUST set decision = fail)" verbatim. | fixed |

## Post-pass review — DACS-2 (two independent cold reviewers, 2026-06-07)

Two fresh-eyes reviewers ran in parallel: (A) meaning-preservation, diffing every hunk old-vs-new; (B) cold internal-consistency read of the whole module.

- **Reviewer A: CLEAN (0 critical).** Confirmed every restructured hunk behavior-preserving — no dropped/weakened/strengthened MUST across all rule families; PSP-2 and VPC-4 tables faithfully encode the prose mappings (polarity, pass/fail/error/indeterminate, the counterparty-malformed carve-out); the **#14 oauth→§7.3.2 reference is not dangling** and oauth retains full force (nonce-include MUST + reject-on-mismatch MUST + document-residual MUST); all verbatim values/addresses/separators/formulas intact. Two cosmetic minors: the §7.7.2 `signed_bytes` line rendering (**fixed** — blank line added) and the VP-C3 explanatory gloss (kept — accurate, adds no obligation).
- **Reviewer B: 1 "critical" + 3 minor — all triaged as pre-existing or false-positive, none introduced by this pass.**
  - **"GOV-1 undefined" — FALSE POSITIVE (single-module scope).** GOV-1 *is* defined in **CORE.md §14.7** (steward disclosure); GOV-2/GOV-3 in DACS-2 continue the cross-module family. The diff confirms this pass did not touch GOV numbering. No action.
  - **§7.9 conformance summary omits PSP/WN/GOV/PA families — pre-existing**, untouched by this pass (the §7.9 table maps roles→rule-ranges and predates it). Flag for steward as a possible completeness cleanup; out of scope for a behavior-preserving readability pass.
  - **§7.5.1(a) "MAY retry per recipe.retryClass" loose wording** + **§7.5.1(b)/VP-R4 indeterminate-rationale restatement** — both pre-existing summary-vs-rule wordings, not introduced here. Left as-is.

**Convergence:** the only review action item (the §7.7.2 rendering) is fixed; no normative meaning changed. DACS-2 treated as clean. Pre-existing items (§7.9 family coverage) noted for a future steward cleanup, not this pass.

## Reviewed — correct, no change
- **DACS-1 §6.3.1 "Parsers … SHOULD emit lowercase" then CF-2 "Scheme lowercased (MUST)".** Looked contradictory but is intentional scoping (permissive default → MUST at the hash/sign/compare boundary). CF-2 already flags the escalation. No fix; possibly a one-line inline note if it keeps tripping readers.

## Notes
- PL fixes belong to the plain-language pass on abstracts/motivation/rationale.
- SC fixes are structure-only on normative prose — run them through the rule-ID + validator gate (no rule text altered).
- Append new finds as `#N | location | PL/SC | issue | fix | open` while reading.
